<?xml version="1.0" encoding="UTF-8"?>
<!--Mybatis配置文件-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.titxu.blog.dao.mapper.TagMapper">
    <!--    List<Tag> findTagsByArticleId(Long articleId); -->
    <select id="findTagsByArticleId" parameterType="long" resultType="com.titxu.blog.dao.pojo.Tag">
        select id,avatar,tag_name as tagName from tit_tag
        where id in
        (select tag_id from tit_article_tag where article_id=#{articleId})
    </select>

    <!--    List<Long> findHotTagIds(int limit); -->
    <select id="findHotTagIds" parameterType="int" resultType="java.lang.Long">
        select tag_id from tit_article_tag
        group by tit_article_tag.tag_id
        order by count(tit_article_tag.tag_id) desc
        limit #{limit}

    </select>
    <!--    List<Tag> findTagsByTagIds(List<Long> tagIds);-->
    <select id="findTagsByTagIds" parameterType="list" resultType="com.titxu.blog.dao.pojo.Tag">
        select id,avatar,tag_name as tagName from tit_tag
        where id in
        <foreach collection="tagIds" item="tagId" separator="," open="(" close=")">
            #{tagId}
        </foreach>

    </select>



</mapper>